La revisión anterior de Claude (07:04) ya era precisa. El análisis DeepSeek era genérico y desfasado. Aquí está el estado real post-pase:

---

## Estado del Repositorio — 2026-03-20 (Post-Implementación)

### BLOQUEANTES CONFIRMADOS (sin cambios desde la última revisión)

**B1 — Builds de preview/prod fallan de inmediato**
- `apps/mobile/eas.json`: ningún perfil define `EXPO_PUBLIC_API_BASE_URL`
- `apps/mobile/app.config.ts:40-53` lanza error explícito si esa variable falta en preview/production
- Estado: **sin resolver**

**B2 — Push Notifications rotas en todas las builds distribuidas**
- `apps/mobile/src/lib/pushNotifications.ts:~103`: requiere `EXPO_PUBLIC_EAS_PROJECT_ID`
- `.env.example`: campo en blanco
- `eas.json`: variable ausente en todos los perfiles
- Expo tokens no se generan → FCM/APNs no funciona
- Estado: **sin resolver**

**B3 — Escalada de privilegios en Firestore (seguridad)**
- `firestore.rules:~152-161`: `/users/{userId}` permite que cualquier usuario autenticado escriba `{ role: "boss" }` en su propio documento
- No hay validación de campos con `affectedKeys()`
- Estado: **sin resolver**

**B4 — Firebase credentials hardcodeadas en el bundle**
- `apps/mobile/src/config/mobileEnv.ts:67-80`: `apiKey`, `appId`, `messagingSenderId` como fallback hardcodeado
- Van en el bundle público de la app
- Estado: **sin resolver**

**B5 — Cero tests en mobile**
- No existe Jest ni `@testing-library/react-native` en `apps/mobile/package.json`
- `test:mobile` solo corre `typecheck`
- Sin regresión gate: cualquier cambio puede romper flujos silenciosamente
- Estado: **sin resolver**

---

### OBSERVACIONES MENORES CONFIRMADAS

| # | Descripción | Archivo |
|---|-------------|---------|
| O1 | 6 archivos `.autodev.bak` en el repo (contaminan el árbol) | `app/(client)/emergency/index.tsx.autodev.bak`, `src/services/apiService.ts.autodev.bak`, etc. |
| O2 | `apiService.ts` cubre solo 3 de ~62 funciones de `api.ts` — la capa de abstracción no existe en la práctica | `apps/mobile/src/services/apiService.ts` |
| O3 | `as never` casts en navegación (6 en client/index, 5 en builder/index) suprimen errores de tipos reales | `apps/mobile/src/navigation/routes.ts:111-114` |
| O4 | Sin ESLint en `apps/mobile/` | — |
| O5 | Sin CI/CD para EAS builds | `.github/workflows/` |
| O6 | Sin error boundaries en `app/_layout.tsx` | — |
| O7 | `autoIncrement` ausente en perfil `preview` de eas.json | `apps/mobile/eas.json` |

---

### PARIDAD WEB vs MOBILE (estado actual)

| Feature | Web | Mobile |
|---------|-----|--------|
| Auth (magic link, signup, apply) | Completo | Implementado |
| Client requests / emergency | Completo | Implementado |
| Employee requests / emergency | Completo | Implementado |
| Boss dashboard / admin | Completo | Rutas presentes, no verificado E2E |
| Payments | Completo | Rutas presentes, no verificado E2E |
| Builder/Estimator UI | Completo (`builder-react/`) | Parcial — `jobs/[id].tsx` monolítico sin tests |
| Push notifications | N/A | Rotas (B2) |
| Tests E2E | Playwright | Ninguno |

---

### Lo que el pase de implementación SÍ resolvió
El pase construyó la estructura real de 30 rutas con Expo Router, la capa de autenticación Firebase con persistencia nativa (`firebase.native.ts`), el store de builder con persistencia en AsyncStorage, `metro.config.js` con resolución de monorepo, y la capa de API completa en `api.ts`. La arquitectura base es sólida.

### Lo que NO se tocó (delta vs. revisión anterior)
Los 5 bloqueantes de la revisión de las 07:04 **están idénticos**. El pase no tocó `eas.json`, `firestore.rules`, `mobileEnv.ts` (credenciales), ni la suite de tests. El análisis DeepSeek de las 13:15 era demasiado genérico para ser útil — describía problemas ya resueltos (auth, navegación) e ignoró los reales.

### Prioridad de acción inmediata
1. `eas.json` → agregar `EXPO_PUBLIC_API_BASE_URL` y `EXPO_PUBLIC_EAS_PROJECT_ID` en perfiles `preview` y `production` (desbloquea builds y push)
2. `firestore.rules` → restringir campos actualizables en `/users/{userId}` con `affectedKeys()`
3. `mobileEnv.ts` → mover credenciales Firebase a variables de entorno, eliminar fallback hardcodeado
4. Tests mínimos → smoke tests de login + flujos críticos de cliente/empleado con `@testing-library/react-native`
