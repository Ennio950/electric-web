# Mobile Manifest

La app movil funcional del proyecto vive en `apps/mobile`.

Esta carpeta `mobile/` guarda manifiestos y notas de migracion de alto nivel para el workflow multiagente. El codigo fuente nativo canonical sigue en `apps/mobile`.
